import { CheckCircle2, Clock3, XCircle } from 'lucide-react';
import { useSearchParams } from 'react-router-dom';
import { money } from '../components/OrderSummary';
import { LinkButton } from '../components/ui';
import type { PaymentResult, PaymentStatus } from '../data/tickets';
import { readStorage } from '../lib/storage';
const states={success:{Icon:CheckCircle2,title:'Tu compra fue confirmada',copy:'Tu reserva quedó registrada correctamente.'},rejected:{Icon:XCircle,title:'No pudimos procesar tu pago',copy:'Revisá los datos o elegí otro método de pago para continuar.'},pending:{Icon:Clock3,title:'Tu pago está pendiente de confirmación',copy:'Te informaremos cuando recibamos la confirmación.'}};
export function CheckoutResultPage(){const[p]=useSearchParams();const status=(p.get('status')??'success') as PaymentStatus;const view=states[status]??states.success;const order=readStorage<PaymentResult|null>('expojuy-demo-order',null,(value):value is PaymentResult=>value!==null&&typeof value==='object'&&'orderId' in value&&'summary' in value);return <main className="result-page"><div className={`result-card ${status}`}><view.Icon/><p className="eyebrow">ESTADO DE LA COMPRA</p><h1>{view.title}</h1><p>{view.copy}</p>{order&&<div className="receipt"><div><span>Número de orden</span><strong>{order.orderId}</strong></div><div><span>Fecha</span><strong>{new Date(order.createdAt).toLocaleString('es-AR')}</strong></div>{order.summary.items.map(i=><div key={i.ticket.id}><span>{i.ticket.name} × {i.quantity}</span><strong>{money(i.ticket.price*i.quantity)}</strong></div>)}<div><span>Total</span><strong>{money(order.summary.total)}</strong></div></div>}<div className="button-row">{status==='rejected'?<><LinkButton to="/checkout">Intentar nuevamente</LinkButton><LinkButton to="/entradas" variant="secondary">Volver a entradas</LinkButton></>:<><LinkButton to="/">Volver al inicio</LinkButton><LinkButton to="/agenda" variant="secondary">Ver agenda</LinkButton></>}</div></div></main>}
